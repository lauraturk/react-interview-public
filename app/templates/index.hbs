<h1>Color Palette</h1>
<NewColor @addCallback={{action "addColor"}} />
<div class="card-container">
  {{#each model as |card|}}
    <ColorCard @card={{card}} @deleteCallback={{action "deleteColor"}}/>
  {{/each}}
</div>

{{outlet}}